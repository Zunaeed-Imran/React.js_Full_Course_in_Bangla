- again use giving the wrong token in the "Display Error Massege."
- i should use like that `{error && <p>{errorMassege}</p>}` but i was doing only {errro}.
- i also didn't use the loading message display in the right way.
- finally i found the problem in the passing url problem. it was ...
- i was passing the url as string 'url' tjat was my fault. it should be like (url)
- 